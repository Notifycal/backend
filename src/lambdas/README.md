Each subfolder within this `lambdas` folder is a lambda function and it requires an index.ts as the entrypoint, and nothing else.

All Lambdas that belong to a given REST API/API Gateway would live in the `api` folder.
